describe('Visible', function ($injector) {
  var Visible;
  beforeEach(module('ooApp'));
  beforeEach(module('ooCalendar'));

  beforeEach(inject(function(_Visible_) {
    Visible = _Visible_;
  }));


  describe('.day=()', function () {
    it('takes a date', function () {

    });

    it('takes a day id and saves that and the right date', function() {

    });
  });
});